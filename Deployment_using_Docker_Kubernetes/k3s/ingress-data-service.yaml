apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: data-service-ingress
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: data-service.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: data-service-service
            port:
              number: 80

